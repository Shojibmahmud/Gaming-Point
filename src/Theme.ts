import { extendTheme, ThemeConfig } from "@chakra-ui/react";

const config: ThemeConfig = {
	initialColorMode: "dark",
};

const Customized_Theme = extendTheme({ config });

export default Customized_Theme;
